<% if form && feedback %>
  <%= form.fields_for :ratings, rating do |f| %>
    <%= render 'track_contents', track: track, position: position, form: f, feedback: feedback %>
  <% end %>
<% else %>
  <%= render 'track_contents', track: track, position: position, form: nil, feedback: nil %>
<% end %>